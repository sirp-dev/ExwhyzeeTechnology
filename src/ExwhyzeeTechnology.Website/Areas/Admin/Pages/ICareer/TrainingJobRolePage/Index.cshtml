@page
@model ExwhyzeeTechnology.Website.Areas.Admin.Pages.ICareer.TrainingJobRolePage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<div class="card card-default">
    <div class="card-body">
        <h1>Index</h1>

        <p>
            <a asp-page="Create">Create New</a>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.CareerTrainingJobRole[0].Title)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CareerTrainingJobRole[0].Description)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CareerTrainingJobRole[0].Disable)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.CareerTrainingJobRole)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Title)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Disable)
                        </td>
                        <td>
                            <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                            <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>        </div>